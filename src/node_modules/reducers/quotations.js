import {
    FETCH_QUOTATIONS_SUCCESS
 } from 'actionTypes'

const initialState = {};

export default (state = initialState, {type, payload}) => {
    switch (type) {
        case FETCH_QUOTATIONS_SUCCESS:
            return {
                ...state,
                items: payload
            }
        default:
            return state;
    }
    
}